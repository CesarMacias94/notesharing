<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/notesharingdb</property>
        <property name="hibernate.connection.username">notesharingDB</property>
        <property name="hibernate.connection.password">Notesharing.db</property>

        <!-- Specify session context -->
        <property name="hibernate.current_session_context_class">org.hibernate.context.internal.ThreadLocalSessionContext</property>

        <!-- List of XML mapping files -->
        <mapping class="bean.User"></mapping>
        <mapping class="bean.Post"></mapping>
        <mapping class="bean.Course"></mapping>

    </session-factory>

</hibernate-configuration>